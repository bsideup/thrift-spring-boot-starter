machine:
  timezone:
    Europe/Tallinn
  java:
    version: oraclejdk7

dependencies:
  cache_directories:
    - ~/.gradle
    - ~/.ivy2
    - ~/.m2
  override:
    - ./gradlew resolveAllDependencies

test:
  override:
    - ./gradlew test