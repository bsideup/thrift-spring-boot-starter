machine:
  timezone:
    Europe/Tallinn
  java:
    version: oraclejdk7
  environment:
    TERM: dumb

dependencies:
  cache_directories:
    - ~/.gradle
    - ~/.ivy2
    - ~/.m2
  override:
    - ./gradlew resolveAllDependencies

test:
  override:
    - ./gradlew test